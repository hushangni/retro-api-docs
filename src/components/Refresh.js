import React, { Component } from 'react';

class Refresh extends Component {
    render() {
        return (
            <div>
                Refresh token
            </div>
        );
    }
};

export default Refresh;